export enum Tool {
    Placement = 'placement',
    Objects = 'objects',
    Erase = 'erase',
}

export enum GridSize {
    SMALL = 10,
    MEDIUM = 15,
    LARGE = 20,
}

export enum ToolOption {
    Placement = 'placement',
    Objects = 'objects',
}
